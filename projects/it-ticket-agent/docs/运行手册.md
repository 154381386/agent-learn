# 运行手册

## 项目边界

- 编排服务：`projects/it-ticket-agent`
- RAG 服务：`projects/it-ticket-rag-service`

## 启动顺序

```bash
cd /Users/lyb/workspace/agent-learn/projects/it-ticket-rag-service
make run
```

```bash
cd /Users/lyb/workspace/agent-learn/projects/it-ticket-agent
make run-cicd-mcp
```

```bash
cd /Users/lyb/workspace/agent-learn/projects/it-ticket-agent
make run-orchestrator
```

或者直接在编排项目里：

```bash
cd /Users/lyb/workspace/agent-learn/projects/it-ticket-agent
make dev
```

如果希望 Orchestrator 走 Runtime Postgres：

```bash
cd /Users/lyb/workspace/agent-learn/projects/it-ticket-agent
make pg-up
make run-orchestrator-pg
```

或者直接：

```bash
cd /Users/lyb/workspace/agent-learn/projects/it-ticket-agent
make dev-pg
```

## 健康检查

```bash
curl http://localhost:8200/healthz
curl http://localhost:8900/healthz
curl http://localhost:8000/healthz
```

Runtime Postgres 检查：

```bash
cd /Users/lyb/workspace/agent-learn/projects/it-ticket-agent
make pg-ps
make pg-logs
```

## RAG 运维

RAG 索引重建、pgvector 启停、知识库文档维护，请到：
- `projects/it-ticket-rag-service/README.md:1`
- `projects/it-ticket-rag-service/docs/rag-production-plan.md:1`

## Runtime 存储迁移

把现有 SQLite 数据迁到 Runtime Postgres：

```bash
cd /Users/lyb/workspace/agent-learn/projects/it-ticket-agent
uv run python scripts/migrate_sqlite_to_postgres.py \
  --sqlite-path ./data/approvals.db \
  --postgres-dsn postgresql://app:app@127.0.0.1:5433/it_ticket_agent_runtime
```

详细步骤见：
- [存储迁移计划](/Users/lyb/workspace/agent-learn/projects/it-ticket-agent/docs/存储迁移计划.md)
